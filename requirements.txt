# ==============================================================================
# Koili TMS RAG — REQUIREMENTS (fully local, no external APIs)
# ==============================================================================

# --- API & WEB ---
fastapi
uvicorn[standard]
python-dotenv
requests
streamlit
numpy
pytz
nest-asyncio

# --- LLM (local, OpenAI-compatible endpoint: Ollama / llama.cpp / vLLM) ---
langchain
langgraph
langchain-openai          # OpenAI-compatible client -> local Ollama /v1
langchain-community
pydantic>=2.0.0

# --- EMBEDDINGS & RETRIEVAL (local) ---
# Embeddings run through Ollama (langchain-openai above).
qdrant-client             # embedded (local dir) vector store
flashrank                 # local ONNX cross-encoder reranker

# --- OBJECT STORAGE (screenshots) ---
minio                     # this project's own MinIO / S3-compatible bucket

# --- DOCUMENT PARSING ---
python-docx
python-pptx
pypdf
pdfplumber
beautifulsoup4

# --- OBSERVABILITY ---
logfire[fastapi,requests]
loguru
